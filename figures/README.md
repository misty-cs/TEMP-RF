# Figures

`results.json` contains the window-sweep values reported in the ICC draft.
Run:

```bash
python3 make_figures.py
```

The script writes PDF and PNG files to `figures/out/`, which is ignored by Git.
